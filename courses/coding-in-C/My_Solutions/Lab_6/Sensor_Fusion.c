#include <stdio.h>

int main() {
    struct Car {
        int fuel_level;
        float max_fuel_level;
        char model[];
    }
    
    void refuel() {
        jej
    }





    FILE *data1 = fopen("text.txt", "r");

    char content[100];

    fgets(content, 100, data1);

    printf("%s", content);

    fclose(data1);

    return 0;
}